import { LockKeyhole } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import { products } from "@/lib/products";

const fields = ["First name", "Last name", "Country / Region", "Company (optional)", "Street address", "Town / City", "State", "Postal code"];

export default function CheckoutPage() {
  return <main className="mx-auto max-w-content px-5 py-10 sm:px-8 sm:py-14 lg:px-0 lg:py-16"><div className="mb-12"><div className="mb-5 flex flex-wrap gap-2 text-sm text-muted"><Link href="/">Home</Link><span>/</span><Link href="/cart">Add to cart</Link><span>/</span><span className="text-ink">Check Out</span></div><h1 className="font-core text-[34px] font-semibold text-ink">Check Out</h1></div><div className="grid gap-12 lg:grid-cols-[1fr_400px]"><section><h2 className="font-core text-2xl font-semibold text-ink">Billing Details</h2><form className="mt-8 grid gap-6 sm:grid-cols-2">{fields.map((field, index) => <label key={field} className={`grid gap-2 text-sm font-semibold text-ink ${index === 4 ? "sm:col-span-2" : ""}`}><span>{field}</span>{field === "Country / Region" || field === "State" ? <select className="h-14 rounded-soft bg-canvas px-5 text-sm font-normal text-muted outline-none"><option>{field === "State" ? "Select state" : "United States"}</option><option>United Kingdom</option><option>Canada</option></select> : <input required={index !== 3} placeholder={`Enter ${field.toLowerCase()}`} className="h-14 rounded-soft bg-canvas px-5 text-sm font-normal text-muted outline-none ring-accent focus:ring-1" />}</label>)}<label className="flex items-center gap-3 text-sm text-muted sm:col-span-2"><input type="checkbox" className="size-4 accent-accent" /> Save this information for next time</label><div className="border-t border-line/50 pt-8 sm:col-span-2"><h2 className="font-core text-2xl font-semibold text-ink">Shipping Method</h2><label className="mt-5 flex items-center gap-4 rounded-soft bg-canvas p-5"><input type="radio" name="shipping" defaultChecked className="size-4 accent-accent" /><span className="flex-1"><strong className="block text-ink">Arrives by 25 Jan</strong><span className="text-sm text-muted">Delivery within 5-7 business days</span></span><strong className="text-ink">FREE</strong></label></div><div className="border-t border-line/50 pt-8 sm:col-span-2"><h2 className="font-core text-2xl font-semibold text-ink">Payment Method</h2><div className="mt-5 rounded-soft bg-canvas p-5"><label className="flex items-center gap-4 font-semibold"><input type="radio" name="payment" defaultChecked className="size-4 accent-accent" /> Credit Card</label><div className="mt-5 grid gap-4 sm:grid-cols-2"><input placeholder="Card number" className="h-12 rounded-soft border border-line/60 bg-white px-4 text-sm outline-none sm:col-span-2" /><input placeholder="MM / YY" className="h-12 rounded-soft border border-line/60 bg-white px-4 text-sm outline-none" /><input placeholder="CVC" className="h-12 rounded-soft border border-line/60 bg-white px-4 text-sm outline-none" /></div></div></div><button className="mt-8 flex h-14 w-full items-center justify-center gap-2 rounded-soft bg-accent px-8 font-semibold text-white sm:w-fit"><LockKeyhole size={17} /> Place order</button></form></section><aside className="h-fit rounded-card p-6 shadow-card sm:p-7"><h2 className="font-core text-2xl font-semibold text-ink">Order Summary</h2><div className="mt-7 grid gap-5 border-y border-line/50 py-6">{products.slice(0, 3).map((product) => <div key={product.slug} className="flex items-center gap-4"><div className="relative size-16 shrink-0 overflow-hidden rounded-soft bg-canvas"><Image src={product.image} alt={product.name} fill sizes="64px" className="object-cover" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-ink">{product.name} x 1</p><p className="mt-1 text-sm text-muted">Color: {product.color}</p></div><span className="text-sm font-bold text-muted">{product.price}</span></div>)}</div><div className="grid gap-4 text-sm"><div className="flex justify-between"><span className="text-muted">Subtotal</span><span className="font-semibold">$197.00</span></div><div className="flex justify-between"><span className="text-muted">Shipping</span><span className="font-semibold">FREE</span></div><div className="mt-3 flex justify-between border-t border-line/50 pt-5 text-lg font-bold"><span>Total</span><span>$197.00</span></div></div></aside></div></main>;
}
